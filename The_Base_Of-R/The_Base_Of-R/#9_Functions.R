                                ###Functions###

x <- readline(prompt = "Enter a value")

y <- as.numeric(readline(prompt = "Enter a number"))

##These are some prepared functions.

##Ensuring Functions
firstFunction <- function() {
  a <- as.numeric(readline(prompt = "Enter a number"))
  b <- as.numeric(readline(prompt = "Enter a number"))
  print(a * b)
}

firstFunction()
